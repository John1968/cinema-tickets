{
  "name": "cinema-tickets-javascript",
  "description": "This is a coding exercise which will allow you to demonstrate how you code and your approach to a given problem.",
  "version": "1.0.0",
  "engines": {
    "node": ">=16.15.1"
  },
  "type": "module",
  "scripts": {
    "jest:test": "jest --runInBand --verbose --reporters=default --reporters=jest-junit",
    "test": "jest",
    "quality:coverage": "npm run jest:test -- --selectProjects coverage --coverage",
    "quality:lint": "eslint ."
  },
  "devDependencies": {
    "@babel/preset-env": "^7.23.3",
    "@dwp/eslint-config-base": "^7.0.0",
    "@jest/globals": "^29.7.0",
    "eslint": "^8.53.0",
    "jest": "28.1.1",
    "jest-junit": "^16.0.0",
    "mocha": "10.0.0"
  },
  "dependencies": {
    "@dwp/node-logger": "^2.0.1"
  }
}
